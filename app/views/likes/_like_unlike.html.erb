<% if current_user.likes.find_by(note_id: note.id) %>
	<%= link_to "Remove like?", unlike_path(note.id), method: :delete, remote: true, class: 'like-color' %>
<% else %>
  <%= link_to "Like!", like_path(note.id), method: :post, remote: true %>
<% end %>